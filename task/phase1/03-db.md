# 03 - packages/db (Drizzle Schema + Migration)

> 依赖：02-shared-config（迁移依赖 05-docker-services 启动 PG）
> 预估：3h
> 可并行：与 04-validators 同时执行

## 目标

定义业务数据库 Schema（files、tasks），配置 Drizzle ORM 连接本地 PostgreSQL，生成初始 migration。

> 注：Auth 相关表（user/session/account/verification）由 Better-Auth 在 06-better-auth 任务中生成，本任务只负责业务表。

## 步骤

### 3.1 安装依赖

```bash
cd packages/db
bun add drizzle-orm postgres
bun add -d drizzle-kit @types/pg
```

### 3.2 创建 Schema 文件结构

```
packages/db/src/
├── schema/
│   ├── index.ts          # 统一导出
│   ├── auth.ts           # Better-Auth 生成（在 06 任务）
│   ├── files.ts          # 本任务
│   └── tasks.ts          # 本任务
├── client.ts             # db instance
└── index.ts
```

`packages/db/src/schema/files.ts`:

```typescript
import {
  pgTable,
  uuid,
  text,
  bigint,
  jsonb,
  timestamp,
  index,
} from 'drizzle-orm/pg-core';
import { sql } from 'drizzle-orm';
// import { user } from './auth';  // 06 任务完成后启用

export const files = pgTable(
  'files',
  {
    id: uuid('id').primaryKey().defaultRandom(),
    // userId: text('user_id').references(() => user.id, { onDelete: 'set null' }),
    userId: text('user_id'), // 06 任务完成后改为 FK
    filename: text('filename').notNull(),
    originalSize: bigint('original_size', { mode: 'number' }).notNull(),
    storageKey: text('storage_key').notNull(),
    bucket: text('bucket').default('uploads').notNull(),
    mimeType: text('mime_type').notNull(),
    metadata: jsonb('metadata'),
    expiresAt: timestamp('expires_at'),
    deletedAt: timestamp('deleted_at'),
    createdAt: timestamp('created_at').defaultNow().notNull(),
    updatedAt: timestamp('updated_at').defaultNow().notNull(),
  },
  t => ({
    userCreatedIdx: index('files_user_created_idx').on(t.userId, t.createdAt),
    expiresIdx: index('files_expires_idx')
      .on(t.expiresAt)
      .where(sql`expires_at IS NOT NULL`),
  })
);
```

`packages/db/src/schema/tasks.ts`:（按设计文档定义）

### 3.4 配置 drizzle.config.ts

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/schema/*',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
    // 本地：postgresql://utils:utils@localhost:5432/utils_plane
  },
});
```

### 3.5 创建数据库连接工具

`packages/db/src/client.ts`：

- 导出 `db` 实例
- 导出所有 schema 类型

### 3.6 生成 & 执行 Migration

```bash
# 确保本地 PG 已启动（docker compose up -d）
bunx drizzle-kit generate
bunx drizzle-kit migrate
```

DATABASE_URL: `postgresql://utils:utils@localhost:5432/utils_plane`

### 3.7 导出 package 入口

`packages/db/src/index.ts`：

- 导出 schema
- 导出 db client
- 导出类型推断 (InferSelectModel, InferInsertModel)

## 验收标准

- [ ] `bun run build` 无报错
- [ ] Migration 文件已生成
- [ ] 连接本地 PG (`docker compose up -d` 后) `drizzle-kit migrate` 成功
- [ ] 其他 workspace 能 import `@utils-plane/db`
